# C# 项目规范 (TRAE .iderules)
## 基础格式
- 缩进：4 个空格
- 编码：UTF-8
- 换行：LF
- 文件末尾空行

## 命名规范（强制）
- 类、方法、属性、接口：PascalCase
- 私有字段：_camelCase
- 参数、局部变量：camelCase
- 常量：PascalCase
- 文件名 = 类名

## 代码风格
- 大括号独占一行
- 使用 var（类型明确时）
- 方法长度 ≤ 60 行
- 条件嵌套 ≤ 3 层
- 禁止空 catch
- 禁止魔法数字
- 禁止 Console.WriteLine()
- 禁止未使用代码、注释代码

## 架构规范
- 分层：Controller → Service → Repository
- 依赖注入优先
- 禁止在控制器写业务逻辑
- 禁止硬编码密钥、连接字符串
- 异常统一处理

## 输出要求
- 只输出可编译、可直接提交代码
- 不擅自修改项目结构
- 不新增未授权依赖
- 有歧义必须询问