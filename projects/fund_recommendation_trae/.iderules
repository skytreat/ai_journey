# 企业级项目严格规范 (TRAE .iderules)

## 一、基础语言与格式
- 全程使用**简体中文**沟通
- 代码缩进：**2 个空格**，禁止使用 Tab
- 文件编码：**UTF-8**
- 换行符：**LF**
- 行长度限制：**不超过 120 字符**
- 末尾空行：每个文件必须保留**1 个空行**

## 二、命名规范（强制执行）
- 变量/函数/参数：**camelCase** 小驼峰
- 类/接口/组件/枚举：**PascalCase** 大驼峰
- 常量/配置项：**UPPER_SNAKE_CASE** 全大写下划线
- 文件名：**kebab-case** 短横线分隔
- 布尔变量：必须以 is / has / can / should 开头
- 禁止使用拼音、无意义单字符（循环 i/j 除外）

## 三、代码风格与质量
- 必须使用**ESLint / Prettier** 自动格式化
- 禁止使用 var，统一使用 const / let
- 禁止魔法数字、魔法字符串，必须抽为常量
- 条件嵌套**不超过 3 层**
- 函数行数**不超过 80 行**，单一职责
- 必须使用**可选链 ?. / 空值合并 ??** 替代繁琐判断
- 禁止 console.log 提交到代码库
- 禁止死代码、注释代码、未使用变量

## 四、注释与文档规范
- 公共类、方法、复杂逻辑：**必须写注释**
- 接口/函数：使用 JSDoc 规范
- 注释简洁明确，不写废话
- 业务逻辑必须说明**用途、边界、注意事项**

## 五、架构与安全规范
- 严格遵循项目现有架构，禁止随意修改结构
- 禁止在控制器/视图层写业务逻辑
- 数据库操作必须通过 Repository / Service 层
- 敏感信息（密钥、Token、密码）**严禁硬编码**
- 所有用户输入必须**校验与过滤**
- 禁止使用 eval、危险函数、不安全依赖
- 不允许引入未审核的第三方库

## 六、Git / 提交规范
- 提交信息遵循：**type(scope): description**
- 类型：feat / fix / refactor / docs / style / test / chore
- 每次提交**只做一件事**，不混合无关代码
- 提交前必须格式化、检查 lint 错误

## 七、输出行为严格约束
- 只输出**可直接提交、可运行、无报错**的代码
- 不生成冗余代码、不自动重构未要求部分
- 不擅自新增文件、不修改配置文件
- 不添加无关依赖、不改变项目结构
- 有冲突、歧义、风险时**必须主动询问**
- 禁止生成未授权的业务逻辑

## 八、禁止行为（零容忍）
- 禁止硬编码敏感信息、环境变量
- 禁止未捕获异常、空指针风险
- 禁止低效循环、内存泄漏
- 禁止不符合团队规范的写法
- 禁止随意删除、修改他人代码
- 禁止不安全、不规范、不清晰的实现